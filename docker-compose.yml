# ============================================================
# AUTO-GENERATED by generate-compose.sh — DO NOT EDIT MANUALLY
# To add/remove bots, use deploy-bot.sh / remove-bot.sh
# Then run: ./generate-compose.sh
# ============================================================

services:
  openclaw-bot-vu:
    build:
      context: .
      dockerfile: Dockerfile
    image: openclaw-bot:latest
    container_name: openclaw-bot-vu
    environment:
      - TELEGRAM_BOT_TOKEN=${TOKEN_VU}
      - NODE_OPTIONS=--max-old-space-size=1280
    ports:
      - "18789:18789"
    volumes:
      - openclaw-bot-vu:/home/node/.openclaw
      - ./bots/vu/config.json:/config/config.json:ro
    mem_limit: 1536m
    memswap_limit: 11g
    restart: unless-stopped

volumes:
  openclaw-bot-vu:
