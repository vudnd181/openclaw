# ============================================================
# AUTO-GENERATED by generate-compose.sh — DO NOT EDIT MANUALLY
# To add/remove bots, use deploy-bot.sh / remove-bot.sh
# Then run: ./generate-compose.sh
# ============================================================

services:
  openclaw-bot-duc:
    build:
      context: .
      dockerfile: Dockerfile
    image: openclaw-bot:latest
    container_name: openclaw-bot-duc
    environment:
      - TELEGRAM_BOT_TOKEN=${TOKEN_DUC}
      - NODE_OPTIONS=--max-old-space-size=1280
    ports:
      - "18789:18789"
    volumes:
      - openclaw-bot-duc:/home/node/.openclaw
      - ./bots/duc/config.json:/config/config.json:ro
    mem_limit: 1536m
    memswap_limit: 11g
    restart: unless-stopped

  openclaw-bot-vu2:
    build:
      context: .
      dockerfile: Dockerfile
    image: openclaw-bot:latest
    container_name: openclaw-bot-vu2
    environment:
      - TELEGRAM_BOT_TOKEN=${TOKEN_VU2}
      - NODE_OPTIONS=--max-old-space-size=1280
    ports:
      - "18790:18789"
    volumes:
      - openclaw-bot-vu2:/home/node/.openclaw
      - ./bots/vu2/config.json:/config/config.json:ro
    mem_limit: 1536m
    memswap_limit: 11g
    restart: unless-stopped

volumes:
  openclaw-bot-duc:
  openclaw-bot-vu2:
